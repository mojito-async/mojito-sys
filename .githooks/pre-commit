#!/bin/sh
# mojito pre-commit hook — installed with:
#   git config core.hooksPath .githooks     (or: precommit/install-hooks.sh)
# Delegates to the gate script so the hook stays a one-liner.
exec "$(git rev-parse --show-toplevel)/precommit/gate.sh"