# Build artifacts
*.mojopkg
*.mojoc
*.o
*.a
*.so
*.dylib
/build/
tests/spike/.build/

# Spike local outputs
spike/context_switch/out/
spike/context_switch/*.log

# Tooling
.max_cache/
__pycache__/
.DS_Store
tests/s1/*/.build/
tests/s1/*/*/.build/
tests/s2/*/.build/
tests/s2/*/*/.build/
tests/s4/time/.build/
tests/s4/time/monotonic/.build/
tests/s3/*/.build/
tests/s3/*/*/.build/
tests/s5/*/.build/
tests/s5/*/*/.build/
tests/s6/*/.build/
tests/s6/*/*/.build/
benchmark/*/.build/
benchmark/*/*.log
spike/completion/.build/

# Belt-and-suspenders for precommit/test-gate.sh's throwaway sandbox repo
# (issue mojito-async/mojito-async#169). Root cause found and fixed
# (precommit/test-gate.sh now unsets GIT_DIR/GIT_INDEX_FILE/etc before
# creating any sandbox, since those variables -- inherited from a real
# hook invocation -- silently redirected the sandbox's `git add`/`git
# config --local` into THIS repo's index and shared config regardless of
# `git -C`). Kept anyway as cheap insurance: a bare `git add probe.txt`/
# `git add "$sb/probe.txt"` cannot stage an ignored path here without -f,
# which nothing in this tree ever passes.
/probe.txt
