"""mojito_sys.memory - page/VM/stack memory services for mojito (S1, issue #24).

Subpackage scaffold owned by the s1/build lane; implementations land in the
S1 memory lanes. Kept intentionally empty until then.
"""

# comptime: memory exports are added by the s1/memory-* lanes via `alias` /
# `public` definitions in sibling modules.